# Copy to .env.local and fill in. Set these same vars in Vercel → Project → Settings → Environment Variables.
DATABASE_URL="postgresql://user:password@host/db?sslmode=require"
AUTH_SECRET="a-long-random-secret-string"
